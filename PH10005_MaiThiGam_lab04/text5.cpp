#include <stdio.h>
#include <math.h>
int main(){
	int so=0;
	int tong=0;
	do{
		printf ("Nhap so :");
		scanf ("%d",&so);
		    if (so>0){
		    	tong+=so;
		
			}else{
				
			}
	}
	while(so>0);
	
	
	printf ("Tong la:%d",tong);
   
   }


