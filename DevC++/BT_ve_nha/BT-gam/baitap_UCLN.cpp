// ?t chuong trình tìm u?c s? chung l?n nh?t (UCLN) và b?i s? chung nh? nh?t (BCNN) c?a hai s? a và b.
#include <stdio.h>
#include <math.h>
int main(){
	int a,b;
	int UCLN;
	printf ("Nhap so nguyen thu nhat la:");
	scanf ("%d",&a);
	
	printf ("Nhap so nguyen thu hai la :");
	scanf ("%d",&b);
	
	if (a>b){
		printf ("Uoc chung lon nhat cua hai so la: %d",b);
	    return UCLN=b;
	    
	}else{
		printf ("Uoc chung lon nhat cua hai so la: %d",a);
		return UCLN=a;
	
	}
	printf ("Uoc chung lon nhatcua hai so la:%d",UCLN);
   return 1;
   }


